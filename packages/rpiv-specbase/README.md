# rpiv-specbase

Installable Pi package for Specbase-specific presentation. The initial command is:

```text
/spcb:kanban --demo
```

Demo mode uses deterministic fixture snapshots only. It reads no live Specbase store and returns an intent without dispatching an action.
