## 1. Frozen local-delivery workflow
- [x] 1.1 Add workflow-only skill contracts and a lazy `specbase-local-delivery` registration under `packages/rpiv-specbase/`; validate the definition with `validateWorkflow()` before registration.
- [x] 1.2 Implement an atomic per-store/change delivery lease acquired by exclusive creation before capture, with run ownership, terminal cleanup, stale-owner reconciliation, and racing-process rejection; then freeze canonical authorization, stack predecessor, planning artifact paths, task/source units, native command inventory, allowed roots, HEAD, and dirty-path baseline and route underplanned or unsafe starts to durable blocked/replan stops.
- [x] 1.3 Implement serial evidence units before serial task units. Each unit must stay in frozen scope, execute its declared native verification, record the result, and check a task only after its verification passes.
- [x] 1.4 Implement typed local gate, bounded remediation, local-review, local-fix, and re-gate paths. Refuse missing verdicts and planning mismatches rather than widening scope.
- [x] 1.5 Extend the workflow execution host with a local-delivery capability policy, run every delivery child with only required local filesystem/command/Git capabilities, deny push, GitHub, archive, panel, and successor tools at the host boundary, then implement explicit-path atomic commits and final verification that subtract baseline dirt and preserve predecessor/successor boundaries.

## 2. Evidence and native verification
- [x] 2.1 Implement `packages/rpiv-specbase/workflows/local-delivery.test.ts` with disposable repositories for authorization, racing-process lease exclusion, stale-lease recovery, terminal release, evidence-before-task order, native-command failure, bounded repair, baseline dirt, scoped commits, resume, stack blocking, and no remote mutation.
- [x] 2.2 Implement `packages/rpiv-specbase/workflows/specbase-local-delivery.test.ts` to call `validateWorkflow()` and assert typed routes, serial fan-out, bounded back edges, declared handoffs, per-run capability filtering, denied remote/panel/archive tools, and unreachable remote/panel/successor graph paths.
- [x] 2.3 Run both named test files with `npm test -- <path>`, record each command/result, link each source to its requirement set, and distinguish graph-shape proof from disposable-repository outcome proof.
- [x] 2.4 Run strict Specbase change validation, `specbase stack validate deliver-specbase-work-from-an-interactive-pi-kanban-df587c88`, `npm run check:files -- packages/rpiv-specbase`, and affected package tests; record all native results separately from linkage.

## 3. Human-operator UX spike journal
- [x] 3.1 After functional verification, conduct and record a keyboard-only human-operator UX spike journal covering simplicity, user-centered design, visibility, consistency, feedback, clarity, accessibility/keyboard behavior, usability, efficiency, delight, defects, and optional unfixed improvements; distinguish observed evidence from follow-up ideas.