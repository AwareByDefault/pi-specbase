## 1. Review-to-draft-PR workflow
- [x] 1.1 Add workflow-only remote skill contracts and lazy `specbase-draft-pr-delivery` registration under `packages/rpiv-specbase/`; call `validateWorkflow()` before registration.
- [x] 1.2 Capture and preflight canonical authorization, green local-delivery result, current HEAD/branch, dirty baseline, normalized remote/base/head identities, and existing PR state; stop on detached, dirty, ambiguous, or non-green input.
- [x] 1.3 Invoke the installed generated review-panel instrument, materialize its full report into typed `clean | advisory | local-fix | replan` data without changing finding strength, and keep only the declared continued-session seam.
- [x] 1.4 Implement bounded in-scope panel fixes followed by deterministic gate, atomic commit, and panel rerun; prohibit planning-artifact edits as a means of resolving a finding, and publish the exact post-fix `finalVerifiedHead` only after the last deterministic gate passes.
- [x] 1.5 Implement compare-before-mutate non-force push of `finalVerifiedHead`, exact draft-PR lookup/create/reuse against that same remote head, canonical result recording with that commit identity, and refreshed canonical Reviewing observation. Omit ready, merge, archive, deletion, and successor edges.

## 2. Evidence and native verification
- [x] 2.1 Implement `packages/rpiv-specbase/workflows/draft-pr-delivery.test.ts` with disposable Git repositories and fake GitHub state for clean/advisory/local-fix/replan, final-gate failure, absent/equal/fast-forward/diverged remotes, push/PR interruption, duplicate/conflicting PRs, canonical-record retry, and post-PR boundaries.
- [x] 2.2 Implement `packages/rpiv-specbase/workflows/specbase-draft-pr-delivery.test.ts` to call `validateWorkflow()` and prove generated-panel delegation, typed report handoff, bounded routes, exact remote descriptors, canonical Reviewing observation, and no force/merge/archive/successor reachability.
- [x] 2.3 Run both named test files with `npm test -- <path>`, record each command/result, link each source to its requirement set, and distinguish fake-remote outcome proof from real credential/provider availability.
- [x] 2.4 Run strict Specbase change validation, `specbase stack validate deliver-specbase-work-from-an-interactive-pi-kanban-df587c88`, `npm run check:files -- packages/rpiv-specbase`, and affected package tests; record all native results separately from linkage.

## 3. Human-operator UX spike journal
- [x] 3.1 After functional verification, conduct and record a keyboard-only human-operator UX spike journal covering simplicity, user-centered design, visibility, consistency, feedback, clarity, accessibility/keyboard behavior, usability, efficiency, delight, defects, and optional unfixed improvements; distinguish observed evidence from follow-up ideas.